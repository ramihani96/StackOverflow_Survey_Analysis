from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project aims to is to increase the student's enrollment rate and decrease drop-out rate in an IT Educational Institute using a data-driven approach.',
    author='Rami Hani',
    license='',
)
